# 현재 시간을 입력받는다.
a = input()  # 예: "1:59"
h, m = map(int, a.split(':'))  # ':'를 기준으로 분리하고 정수로 변환

# 1시간을 더한다.
h += 1

# 만약 시간이 24 이상이 되면 0으로 초기화 (24시간 형식)
if h == 24:
    h = 0

# 결과를 출력
print(f"{h}:{m}")